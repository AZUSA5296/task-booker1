<body>

    <h1>Editing Book</h1>

    <% if @book.errors.any? %>
    <div id="alert_update">
        <h2><%= @book.errors.count %>errors prohibited this book from being saved:</h2>
        <ul>
              <% @book.errors.full_messages.each do |message| %>
              <li><%= message %></li>
              <% end %>
        </ul>
    </div>
    <% end %>

    <%= form_with model:@book, url:booker_path(@book), local: true do |f| %>

    <h4>Title</h4>
    <%= f.text_field :title %>
    <h4>Body</h4>
    <%= f.text_area :body %>
    <h4><%= f.submit 'Update Book' %></h4>

    <% end %>

    <p id="button2"><%= link_to 'Show', booker_path(@book) %>｜<%= link_to "Back", bookers_path %></p>

</body>



